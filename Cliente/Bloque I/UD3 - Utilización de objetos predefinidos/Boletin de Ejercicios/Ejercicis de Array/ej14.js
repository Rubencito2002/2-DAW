//Crear una función que muestre el contenido de un array, cada elemento en una línea distinta.
function mostrarArrayEnLineas(array) {
    for (let i = 0; i < array.length; i++) {
        console.log(array[i]);
    }
}